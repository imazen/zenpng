[package]
name = "zenpng"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "AGPL-3.0-or-later"
description = "PNG encoding and decoding with zencodec-types trait integration"
categories = ["multimedia::images", "multimedia::encoding"]
keywords = ["png", "image", "encoder", "decoder", "codec"]
repository = "https://github.com/imazen/zenpng"

[dependencies]
thiserror = { version = "2.0", default-features = false }
rgb = { version = "0.8", default-features = false, features = ["as-bytes"] }
imgref = "1.12"
bytemuck = { version = "1.25", default-features = false }
enough = "0.4"
zencodec-types = { path = "../zencodec-types" }
zenquant = { path = "../zenquant", optional = true }
zenflate = { path = "../zenflate" }
linear-srgb = "0.5.5"
zenzop = { path = "../zopfli-fork", optional = true, default-features = false, features = ["zlib", "std"] }
almost-enough = { version = "0.4", features = ["std"] }

[dev-dependencies]
png = "0.18"
archmage = { version = "0.8.4", features = ["std"] }
codec-corpus = "1.0.2"
flate2 = "1"
libdeflater = "1.25"
miniz_oxide = "0.8"
zopfli = "0.8"

[profile.test]
opt-level = 2

[features]
default = ["quantize"]
quantize = ["dep:zenquant"]
unchecked = ["zenflate/unchecked"]
zopfli = ["dep:zenzop"]
